Building the database
Run initDB.sql, add_states.sql, add_datasets.sql
I needed to convert the end-of-line characters on 2015MedianIncomeByCounty.csv over to unix style, you may need to convert to windows or mac style if necessary, IDK.
